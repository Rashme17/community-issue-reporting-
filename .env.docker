# API Configuration
REACT_APP_API_URL=http://localhost:8081/api

# Development server settings
WATCHPACK_POLLING=true
CHOKIDAR_USEPOLLING=true
FAST_REFRESH=true

# Disable browser opening in container
BROWSER=none

# Host settings for Docker
HOST=0.0.0.0
PORT=3000